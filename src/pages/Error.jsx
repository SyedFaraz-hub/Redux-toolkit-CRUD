import React from "react";

const Error = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: `calc(100vh - 60px)`,
      }}
    >
      <h1>Error Page</h1>
    </div>
  );
};

export default Error;
